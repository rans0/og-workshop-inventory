name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Generate wrangler.toml
        run: |
          cat <<EOF > wrangler.toml
          name = "workshop-inventory"
          compatibility_date = "2025-01-01"
          pages_build_output_dir = ".vercel/output/static"

          [[d1_databases]]
          binding = "DB"
          database_name = "workshop-inventory-db"
          database_id = "${{ secrets.CLOUDFLARE_D1_DATABASE_ID }}"
          EOF

      - name: Build
        run: |
          npm run build
          npm run pages:build
        env:
          NEXT_PUBLIC_APP_USERNAME: ${{ secrets.NEXT_PUBLIC_APP_USERNAME }}
          NEXT_PUBLIC_APP_PIN: ${{ secrets.NEXT_PUBLIC_APP_PIN }}

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy .vercel/output/static --project-name=workshop-inventory
